#!/usr/bin/env bash

source "${HOME}/.dotfiles-variables"

git_dir="${PWD}/.git"
hooks_dir="${git_dir}/hooks"

if [[ ! -d "${git_dir}" ]]; then
    echo "folder ${git_dir} does not exist."
    exit 1
fi

rm -rf "${hooks_dir}"
git init --template="${DOTFILES_ROOT}/git/git-templates.symlink"

exit 0
